# HAL9000 dependencies

# Core AI — multi-provider
anthropic>=0.25.0       # Claude
openai>=1.12.0          # GPT-4o + Whisper STT
google-genai>=1.0.0     # Gemini

# Vision
opencv-python>=4.9.0

# Audio — mic recording
pyaudio>=0.2.14
soundfile>=0.12.1
numpy>=1.26.0

# Voice output
edge-tts>=6.1.0         # free TTS (default provider)
elevenlabs>=1.1.0
pygame>=2.5.2           # audio playback

# Web UI
flask>=3.0.0

# Web search
duckduckgo-search>=6.0.0

# MCP server (Claude Code integration)
mcp>=1.0.0

# Free mode — local LLM + local STT (no API keys needed)
# Set FREE_MODE=true in .env to use these
ollama>=0.4.0           # local LLM (Llama 3, Mistral, Phi-3)
faster-whisper>=1.0.0   # local Whisper STT (CTranslate2)

# Local voice cloning (optional — requires Python 3.11)
# TTS>=0.22.0
# torch>=2.5,<2.6
# transformers>=4.40,<4.45

# Licensing (JWT verification)
PyJWT>=2.8.0            # license key decode
cryptography>=41.0.0    # Ed25519 signature verification

# Utilities
python-dotenv>=1.0.0
pillow>=10.2.0          # image encoding
